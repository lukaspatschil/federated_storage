syntax = "proto3";

import "shared.proto";

package pictureStorage;

service PictureStorageService {
  rpc CreatePictureById (stream PictureCreationById) returns (Empty) {}
  rpc GetPictureById (Id) returns (stream PictureData) {}
  rpc RemovePictureById (Id) returns (Empty) {}
}
